/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'boask.fr',
      'images.unsplash.com',
      'dev-thebookplace.s3.eu-west-2.amazonaws.com',
    ],
  },
}

module.exports = nextConfig
